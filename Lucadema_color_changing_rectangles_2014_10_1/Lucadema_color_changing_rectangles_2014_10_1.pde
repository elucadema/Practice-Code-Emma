int x = 0;
int xrect = 25;
void setup() {
  size(500, 500);
  frameRate(5);
}

void draw() {
  if (mouseX <= width && mouseX >= ) {
    for (int x = 0; x <= width; x += xrect) {
      rect(x, 0, x, height);
      
  }


}
